DISCORD_TOKEN=
COMMAND_PREFIX=!
# Point at the Laravel backend, including the /api/v1 prefix.
BACKEND_URL=http://127.0.0.1:8000/api/v1
# Set to "false" to call the real backend, "true" (or unset) to use mock data.
USE_MOCK_DATA=true
GROQ_API_KEY=
GROQ_MODEL=
